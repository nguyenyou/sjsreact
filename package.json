{
  "scripts": {
    "build:dev": "rm -rf main && scala-cli --power package --js --js-mode=dev src -f  -o main",
    "build:prod": "scala-cli --power package --js --js-mode=full src -f  -o main"
  },
  "dependencies": {
    "@awesome.me/webawesome": "3.0.0",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  }
}